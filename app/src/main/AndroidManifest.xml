<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nmd_auth">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PassCheckActivity"></activity>
        <activity android:name=".PasswordActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".OTPCheckActivity" />
        <activity android:name=".LogOutActivity" />
        <activity android:name=".UserDetailsActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LocListActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyANICPotZ0dxfLai4gp0WiRxlsGfRv7sV4" />

        <activity android:name=".LocationActivity" />

        <service android:name=".LocationTrack" />
    </application>

</manifest>